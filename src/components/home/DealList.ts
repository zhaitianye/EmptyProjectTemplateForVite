import {
  ref,
  reactive, // reactive 创建响应式对象
  toRefs, // 响应式对象 转变为ref
  defineComponent,
} from 'vue';
export default defineComponent({
  name: 'DealList',
  setup(props:any,context:any) {
    

    return {
      
    };
  },
});